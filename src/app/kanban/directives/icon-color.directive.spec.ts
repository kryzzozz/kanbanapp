import { IconColorDirective } from './icon-color.directive';

describe('IconColorDirective', () => {
  it('should create an instance', () => {
    const directive = new IconColorDirective();
    expect(directive).toBeTruthy();
  });
});
